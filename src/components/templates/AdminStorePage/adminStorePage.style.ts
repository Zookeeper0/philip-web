import styled from "styled-components";

export const AdminStorePage = styled.div`
  display: flex;
  width: 100%;
  height: 100%;
  border-radius: 4px;
  background: ${(props) => props.theme.colors.white};
`;
